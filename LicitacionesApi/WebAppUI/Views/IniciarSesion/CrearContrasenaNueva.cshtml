@{
    ViewBag.Title = "Contraseña nueva";
    Layout = "~/Views/Shared/_LayoutNoMenu.cshtml";
}

<script src="~/Scripts/View/ContrasenaNuevaHandler.js"></script>

@using (Html.BeginForm("CrearContrasenaNuevaForm", "IniciarSesion", FormMethod.Post, new { id = "crearContrasenaForm" }))
{
    <script src="~/Scripts/View/LocalValidation.js"></script>
    <h2 class="my-5 text-primary">Crear contraseña nueva</h2>

    <div class="container">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="card">
                    <div class="card-header">Digita una contraseña nueva</div>
                    <div class="card-body">
                        <div class="form-group">
                            <label for="contrasenaInput">Correo electrónico</label>
                            @Html.Password("contrasena", new { id = "contrasenaInput", @class = "form-control" })
                            @Html.ValidationMessage("contrasenaVal", "El campo contraseña es requerido.", new { id = "validacionContrasena", @class = "text-danger" })
                        </div>
                        <div class="form-group">
                            <label for="contrasenaTwiceInput">Repetir contraseña</label>
                            @Html.Password("contrasenaTwice", new { id = "contrasenaTwiceInput", @class = "form-control" })
                            @Html.ValidationMessage("contrasenaTwiceVal", "El campo repetir contraseña tiene que coincidir.", new { id = "validacionContrasenaTwice", @class = "text-danger" })
                        </div>
                        <button type="submit" class="btn btn-primary" id="btnLogin" disabled>Enviar</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
}